# QUESTION YOUR PDF
## How to start ??
``` pip install poetry ```
### install dependencies
``` poetry shell ```
### start the virtual env
``` poetry install ```
### create .env and write 
```GOOGLE_API_KEY=```
### Run the code
``` streamlit run rag/__init__.py```
