# ANSWER YOUR PDF
## How to start ??
``` pip install poetry ```
### install dependencies
``` poetry install ```
### start the virtual env
``` poetry shell ```
### Run the code
``` poetry rag/__init__.py```
